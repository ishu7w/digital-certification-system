PORT=3001
# Generate a secret with: openssl rand -hex 32
SIGNING_SECRET=
ADMIN_EMAIL=
# Generate using the command in README.md. Never place a plain password here.
ADMIN_PASSWORD_HASH=
# Read-only sample workspace. Disable before hosting private certificates.
DEMO_MODE=true
